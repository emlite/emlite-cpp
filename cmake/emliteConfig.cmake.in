@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@EMLITE_WITH_EMS_ENV@)
  find_dependency(ems_env)
endif()

if(@EMLITE_WITH_DLMALLOC@)
  find_dependency(dlmalloc)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/emliteTargets.cmake")

set(emlite_VERSION "@PACKAGE_VERSION@")